version: '3'
services:
  auth:
    environment:
      UVICORN_RELOAD: 1
      DEBUG: 1
    volumes:
      - ../../src:/app
    ports:
      - '5000:5000'
    command: python3 main.py

  tests:
    volumes:
      - ./:/app
