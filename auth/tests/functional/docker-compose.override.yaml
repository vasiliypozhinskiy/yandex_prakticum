version: '3'
services:
  auth:
    environment:
      UVICORN_RELOAD: 1
      DEBUG: 1
    volumes:
      - ../../src:/app
    ports:
      - '5000:5000'
    command: bash -c "/wait && python3 create_superuser.py && python3 main.py"

  tests:
    volumes:
      - ./:/app
